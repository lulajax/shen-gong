# 多智能体协同功能 - 需求与实施文档

## 项目概述

为 Shen Gong (神工) 平台添加多智能体协同处理任务的能力,支持复杂任务的自动分解、多 Agent 协作执行和结果智能聚合。

---

## 功能需求

### 1. 协同模式
- **混合模式**: 支持串行、并行、层级三种执行策略
  - 串行执行(Sequential): Agent按顺序执行,前一个的输出作为下一个的输入
  - 并行执行(Parallel): 多个Agent同时处理任务的不同部分,最后聚合结果
  - 层级执行(Hierarchical): 主Agent分解任务并分配给子Agent,然后整合结果

### 2. 任务分解策略
- **LLM智能分解**: 使用大语言模型自动分析复杂任务并拆分为子任务
- **自动Agent选择**: 根据任务类型和业务域自动选择最合适的Agent
- **依赖关系识别**: 自动识别子任务之间的依赖关系
- **参数映射**: 智能映射前置任务的输出到后续任务的输入

### 3. 应用场景
- **数据分析管道**: 数据获取 → 处理 → 分析 → 可视化的完整流程
- **复杂查询**: 需要多个数据源和多步推理的查询
- **任务分工**: 将大任务拆分给多个专业Agent并行处理

### 4. 触发方式
- **自动检测**: LLM自动判断用户输入是否需要多Agent协同
- **显式触发**: 通过专用API端点强制使用多Agent模式
- **混合模式**: 默认自动检测,也提供显式触发接口

### 5. 执行模式
- **同步执行**: 短任务立即返回完整结果
- **异步执行**: 长任务返回taskId,客户端轮询结果(第二期)

---

## 核心实现(第一期 - 已完成)

### Phase 1: 数据模型 ✅

#### 枚举类(2个)
- `ExecutionMode.java` - 执行模式枚举(SEQUENTIAL, PARALLEL, HIERARCHICAL)
- `SubTaskStatus.java` - 子任务状态枚举(PENDING, RUNNING, COMPLETED, FAILED)

#### 数据模型类(6个)
- `SubTaskExecution.java` - 子任务执行记录
- `SubTask.java` - 子任务模型(含依赖关系、参数映射、重试配置)
- `ExecutionPlan.java` - 执行计划(含循环依赖检测)
- `SubTaskResult.java` - 子任务执行结果
- `MultiAgentResult.java` - 多Agent执行结果
- `MultiAgentTask.java` - 多Agent任务

#### 协同上下文
- `CollaborationContext.java` - 协同上下文管理器
  - 支持JSONPath风格数据提取(`task-1.data.metrics.gmv`)
  - 线程安全的结果存储(ConcurrentHashMap)
  - 完整的执行历史记录

#### 数据库层(3个)
- `TaskExecutionEntity.java` - 扩展(添加parentTaskId, isSubTask, taskOrder)
- `MultiAgentExecutionEntity.java` - 多Agent执行记录实体
- `MultiAgentExecutionRepository.java` - Repository接口
- `V2__add_multi_agent_support.sql` - 数据库迁移脚本

### Phase 2: 核心组件 ✅

#### 1. TaskOrchestrator (任务编排器)
**文件**: `src/main/java/com/shengong/agentruntime/collaboration/TaskOrchestrator.java`

**核心功能**:
- `needsCollaboration()` - 使用LLM判断是否需要多Agent协同
- `decompose()` - 使用LLM智能分解任务为子任务
- 执行计划验证(最多重试3次)
- 降级策略(失败时使用GenericAnalysisAgent)

**关键特性**:
- Few-shot Learning提供示例
- 自动Agent存在性验证
- 循环依赖检测
- 完整的错误处理

#### 2. ExecutionCoordinator (执行协调器)
**文件**: `src/main/java/com/shengong/agentruntime/collaboration/ExecutionCoordinator.java`

**核心功能**:
- 串行执行策略(第一期实现)
- 拓扑排序(Kahn算法)处理依赖关系
- 参数提取和映射
- 重试机制(指数退避)
- 执行历史记录

**关键特性**:
- 自动处理任务依赖
- 失败任务自动重试(最多3次)
- 可选任务失败不影响整体
- 完整的执行追踪

#### 3. ResultAggregator (结果聚合器)
**文件**: `src/main/java/com/shengong/agentruntime/collaboration/ResultAggregator.java`

**核心功能**:
- 使用LLM生成综合总结
- 处理全部成功、部分失败、全部失败三种情况
- 降级处理(LLM失败时使用模板聚合)

**关键特性**:
- 智能结果整合
- 执行流程可视化
- 友好的错误提示

### Phase 3: 服务集成 ✅

#### 1. MultiAgentCollaborationService (门面服务)
**文件**: `src/main/java/com/shengong/agentruntime/collaboration/MultiAgentCollaborationService.java`

**核心功能**:
- 整合TaskOrchestrator、ExecutionCoordinator、ResultAggregator
- 管理执行生命周期
- 持久化执行记录
- 提供预览计划功能

#### 2. IntelligentAgentRouter (智能路由器 - 已增强)
**修改**: `src/main/java/com/shengong/agentruntime/service/IntelligentAgentRouter.java`

**新增功能**:
- 自动检测是否需要多Agent协同
- 创建MultiAgentTask
- 降级处理(失败时回退到单Agent)

#### 3. RouterAgentService (路由服务 - 已增强)
**修改**: `src/main/java/com/shengong/agentruntime/service/RouterAgentService.java`

**新增功能**:
- 识别MultiAgentTask
- 委托给MultiAgentCollaborationService执行

#### 4. ChatController (控制器 - 已增强)
**修改**: `src/main/java/com/shengong/agentruntime/controller/ChatController.java`

**新增端点**:
- `POST /api/v1/chat/send` - 自动检测模式(已增强)
- `POST /api/v1/chat/preview-plan` - 预览执行计划(不执行)
- `POST /api/v1/chat/send-multi` - 强制多Agent模式

---

## API使用示例

### 1. 自动检测模式
```bash
POST /api/v1/chat/send
{
  "userId": "user123",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "分析昨天直播间的表现,包括GMV、观看人数、互动率"}
      ]
    }
  ]
}
```

**系统行为**:
- LLM自动判断需要多Agent协同
- 生成执行计划:
  - task-1: LiveDataFetchAgent (获取直播数据)
  - task-2: LiveDataPrepAgent (计算指标)
  - task-3: LiveAnalysisAgent (生成分析报告)
- 串行执行并聚合结果

### 2. 预览执行计划
```bash
POST /api/v1/chat/preview-plan
{
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "对比分析直播和订单数据"}
      ]
    }
  ]
}
```

**返回**:
```json
{
  "plan": {
    "planId": "plan-abc123",
    "executionMode": "SEQUENTIAL",
    "reason": "需要获取两个数据源并进行对比分析",
    "subTasks": [
      {
        "taskId": "task-1",
        "agentName": "LiveDataFetchAgent",
        "description": "获取直播数据",
        ...
      },
      {
        "taskId": "task-2",
        "agentName": "OrderDataAgent",
        "description": "获取订单数据",
        ...
      }
    ]
  },
  "success": true
}
```

### 3. 强制多Agent模式
```bash
POST /api/v1/chat/send-multi
{
  "userId": "user123",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "分析订单数据"}
      ]
    }
  ]
}
```

---

## 技术实现细节

### 1. 数据路径提取(JSONPath风格)
```java
// 支持的路径格式
"task-1.data.metrics.gmv"        // 提取特定字段
"task-1.data"                     // 提取整个data对象
"task-1.summary"                  // 提取summary字段
```

### 2. 拓扑排序(依赖解析)
使用Kahn算法对子任务进行拓扑排序,确保按正确顺序执行:
```java
// 构建依赖图和入度
Map<String, Integer> inDegree;
Map<String, List<String>> graph;

// 拓扑排序
Queue<String> queue; // 入度为0的任务
List<SubTask> sorted; // 排序结果
```

### 3. 重试机制
```java
// 指数退避重试
for (int attempt = 0; attempt <= maxRetries; attempt++) {
    AgentResult result = executeAgent(task);
    if (result.isSuccess()) return result;
    Thread.sleep(1000L * (attempt + 1)); // 1s, 2s, 3s...
}
```

### 4. 执行追踪
完整记录每个子任务的执行历史:
- 开始时间、完成时间
- 执行状态、重试次数
- 错误信息、执行耗时

---

## 数据库设计

### 1. multi_agent_execution表
```sql
CREATE TABLE multi_agent_execution (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    parent_task_id VARCHAR(100) NOT NULL UNIQUE,
    execution_plan_json TEXT,
    execution_mode VARCHAR(50),
    total_latency_ms INT,
    success_count INT,
    failure_count INT,
    status VARCHAR(20),
    user_id VARCHAR(100),
    created_at DATETIME,
    completed_at DATETIME,
    ...
);
```

### 2. task_execution表扩展
```sql
ALTER TABLE task_execution
ADD COLUMN parent_task_id VARCHAR(100),
ADD COLUMN is_sub_task BOOLEAN DEFAULT FALSE,
ADD COLUMN task_order INT;
```

### 3. 视图: v_multi_agent_task_detail
提供多Agent任务及其子任务的完整视图。

---

## 后续扩展计划(第二期)

### 1. 并行执行策略 🔜
**目标**: 多个独立任务同时执行

**实现要点**:
- 使用线程池(ExecutorService)
- Semaphore限流(避免资源耗尽)
- CompletableFuture异步编排
- 超时控制

**示例场景**:
```
用户: "对比分析直播和订单数据"

并行执行:
├── task-1a: LiveDataFetchAgent (获取直播数据)
└── task-1b: OrderDataAgent (获取订单数据)
     ↓
task-2: GenericAnalysisAgent (综合分析)
```

**实现文件**:
- `ParallelExecutionStrategy.java`

### 2. 层级执行策略 🔜
**目标**: 主Agent动态调用子Agent

**实现要点**:
- 定义OrchestratorAgent接口
- 主Agent可调用`collaborationService.executeSubTasks()`
- 支持动态决策子任务

**示例场景**:
```
用户: "生成本月运营报告"

主Agent: ReportOrchestratorAgent
├── 分析报告需求
├── 决定需要哪些数据
└── 动态调用子Agent
    ├── LiveDataAgent
    ├── OrderDataAgent
    └── UserDataAgent
```

**实现文件**:
- `HierarchicalExecutionStrategy.java`
- `OrchestratorAgent.java` (接口)

### 3. 异步执行模式 🔜
**目标**: 长任务不阻塞HTTP请求

**实现要点**:
- 立即返回taskId
- 客户端轮询`GET /api/v1/tasks/{taskId}`
- Redis缓存执行结果
- 支持超时时间配置

**API设计**:
```bash
# 发起异步任务
POST /api/v1/chat/send-async
返回: {"taskId": "task-12345", "status": "processing"}

# 查询任务状态
GET /api/v1/tasks/task-12345
返回: {"taskId": "task-12345", "status": "completed", "result": {...}}
```

**实现文件**:
- `AsyncTaskService.java`
- `TaskStatusCache.java` (Redis)

### 4. 执行进度推送 🔜
**目标**: 实时推送子任务执行进度

**实现方式**:
- WebSocket推送
- 或SSE(Server-Sent Events)

**消息格式**:
```json
{
  "taskId": "task-12345",
  "type": "progress",
  "currentSubTask": "task-2",
  "completedCount": 1,
  "totalCount": 3,
  "message": "正在计算指标..."
}
```

**实现文件**:
- `TaskProgressWebSocketHandler.java`
- `ProgressNotificationService.java`

### 5. 智能执行计划优化 🔜
**目标**: 基于历史数据优化执行计划

**实现要点**:
- 收集历史执行数据
- 分析常见任务模式
- 缓存高频执行计划
- 自动优化子任务顺序

**实现文件**:
- `ExecutionPlanOptimizerService.java`
- `TaskPatternAnalyzer.java`

### 6. 动态重试策略 🔜
**目标**: 根据错误类型选择重试策略

**策略类型**:
- 立即重试: 临时性错误(网络波动)
- 指数退避: 服务繁忙
- 不重试: 参数错误、权限错误

**实现文件**:
- `RetryStrategySelector.java`

### 7. 子任务结果缓存 🔜
**目标**: 避免重复执行相同子任务

**实现要点**:
- Redis缓存子任务结果
- 基于参数计算缓存key
- 设置合理的过期时间
- 支持强制刷新

**实现文件**:
- `SubTaskResultCache.java`

---

## 配置建议

### application.yml
```yaml
# 多智能体协同配置
multi-agent:
  # 执行配置
  execution:
    max-concurrent: 5          # 最大并发子任务数(并行模式)
    default-timeout: 300000    # 默认超时时间(毫秒)
    max-retries: 3             # 最大重试次数

  # LLM配置
  llm:
    decomposition-timeout: 30000  # 任务分解超时
    aggregation-timeout: 30000    # 结果聚合超时

  # 缓存配置
  cache:
    enabled: false             # 是否启用结果缓存(第二期)
    ttl: 3600                  # 缓存过期时间(秒)
```

---

## 性能优化建议

### 1. LLM调用优化
- 使用流式响应(Streaming)减少延迟
- 缓存常见任务的分解结果
- 并行调用多个LLM(分解和参数提取)

### 2. 数据库优化
- 使用批量插入子任务记录
- 异步更新执行记录
- 定期清理历史数据

### 3. 并发控制
- Semaphore限制并发数
- 线程池大小根据CPU核心数调整
- 使用虚拟线程(Java 21+)

---

## 监控指标

### 关键指标
- 多Agent任务成功率
- 平均执行时长
- 子任务失败率
- LLM调用延迟
- 并发任务数

### 查询示例
```sql
-- 统计多Agent任务成功率
SELECT
    status,
    COUNT(*) as count,
    AVG(total_latency_ms) as avg_latency,
    AVG(success_count) as avg_success,
    AVG(failure_count) as avg_failure
FROM multi_agent_execution
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY status;

-- 查询最耗时的任务
SELECT
    parent_task_id,
    execution_mode,
    total_latency_ms,
    success_count + failure_count as total_tasks
FROM multi_agent_execution
ORDER BY total_latency_ms DESC
LIMIT 10;
```

---

## 常见问题(FAQ)

### Q1: 如何判断任务是否需要多Agent协同?
A: LLM会根据以下标准判断:
- 是否需要多个数据源
- 是否有多个明确步骤
- 是否需要多个专业能力
- 关键词:"对比"、"综合"、"分析...和..."

### Q2: 任务分解失败怎么办?
A: 系统有完善的降级策略:
1. 重试3次
2. 失败后降级使用GenericAnalysisAgent
3. 记录错误日志供排查

### Q3: 如何调试多Agent任务?
A: 提供多种调试方式:
1. 使用`/preview-plan`预览执行计划
2. 查看`multi_agent_execution`表的执行记录
3. 查看`task_execution`表的子任务详情
4. 检查CollaborationContext的执行历史

### Q4: 子任务失败会影响整体吗?
A: 取决于任务配置:
- 必须任务(optional=false): 失败会终止后续执行
- 可选任务(optional=true): 失败不影响其他任务

### Q5: 如何扩展新的执行策略?
A: 实现ExecutionStrategy接口:
```java
public interface ExecutionStrategy {
    List<SubTaskResult> execute(List<SubTask> tasks,
                                CollaborationContext context);
}
```

---

## 测试建议

### 1. 单元测试
- TaskOrchestrator: 模拟LLM响应,验证分解逻辑
- ExecutionCoordinator: 测试拓扑排序、重试机制
- ResultAggregator: 验证结果聚合逻辑
- CollaborationContext: 测试数据路径提取

### 2. 集成测试
- 端到端测试(真实LLM调用)
- 并发执行测试
- 失败场景测试(网络错误、超时)
- 数据传递正确性测试

### 3. 性能测试
- 并发负载测试
- 长任务执行测试
- 资源消耗监控

---

## 部署清单

### 部署前检查
- [ ] 执行数据库迁移脚本
- [ ] 配置LLM API密钥
- [ ] 调整超时和重试参数
- [ ] 配置日志级别
- [ ] 检查依赖注入是否正确

### 验证步骤
1. 启动应用: `mvn spring-boot:run`
2. 检查健康状态: `curl http://localhost:8080/actuator/health`
3. 测试预览计划: 调用`/preview-plan`端点
4. 测试自动检测: 发送复杂任务到`/send`端点
5. 检查数据库记录: 查询`multi_agent_execution`表

---

## 文档版本

- **创建日期**: 2025-12-18
- **最后更新**: 2025-12-18
- **版本**: 1.0
- **作者**: Shen Gong开发团队

---

## 相关文档

- [多智能体协同实施计划](/home/work/.claude/plans/zazzy-noodling-moonbeam.md)
- [CLAUDE.md - 项目指南](/home/work/project/shen-gong/CLAUDE.md)
- [API文档](待补充)
