# 多智能体协同功能 - 测试与验证指南

## 概述

本文档提供多智能体协同功能的测试方法、API使用示例和验证步骤。

---

## 一、环境准备

### 1. 启动应用

```bash
cd /home/work/project/shen-gong
mvn spring-boot:run
```

### 2. 验证应用启动

```bash
curl http://localhost:8080/actuator/health
```

预期响应：
```json
{"status":"UP"}
```

### 3. 数据库检查

确认数据库迁移脚本已执行，检查新增的表：

```sql
-- 检查多Agent执行记录表
SHOW TABLES LIKE 'multi_agent_execution';

-- 检查task_execution表的新字段
DESC task_execution;

-- 检查视图
SHOW CREATE VIEW v_multi_agent_task_detail;
```

---

## 二、API测试

### 测试1: 自动检测多Agent协同

**场景**：复杂任务自动触发多Agent模式

**请求**：
```bash
curl -X POST http://localhost:8080/api/v1/chat/send \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test-user-001",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "分析昨天直播间的表现，包括GMV、观看人数、互动率等指标，并与前天对比"
          }
        ]
      }
    ]
  }'
```

**预期行为**：
1. IntelligentAgentRouter判断需要多Agent协同
2. TaskOrchestrator分解任务为多个子任务
3. ExecutionCoordinator串行执行各子任务
4. ResultAggregator聚合结果返回

**验证点**：
- 响应中应包含多个Agent的执行结果
- 响应时间较长（因为串行执行多个Agent）
- 数据库`multi_agent_execution`表应有新记录

### 测试2: 预览执行计划（不执行）

**场景**：查看任务会如何被分解，但不实际执行

**请求**：
```bash
curl -X POST http://localhost:8080/api/v1/chat/preview-plan \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "对比分析直播和订单数据，生成运营报告"
          }
        ]
      }
    ]
  }'
```

**预期响应**：
```json
{
  "plan": {
    "planId": "plan-abc123",
    "executionMode": "SEQUENTIAL",
    "reason": "需要获取两个数据源并进行综合分析",
    "subTasks": [
      {
        "taskId": "task-1",
        "agentName": "LiveDataFetchAgent",
        "taskType": "live_data_fetch",
        "domain": "live",
        "description": "获取直播数据",
        "dependencies": [],
        "dataMappings": {},
        "staticParams": {}
      },
      {
        "taskId": "task-2",
        "agentName": "OrderDataAgent",
        "taskType": "order_data_fetch",
        "domain": "order",
        "description": "获取订单数据",
        "dependencies": [],
        "dataMappings": {},
        "staticParams": {}
      },
      {
        "taskId": "task-3",
        "agentName": "GenericAnalysisAgent",
        "taskType": "analysis",
        "domain": "generic",
        "description": "综合分析并生成报告",
        "dependencies": ["task-1", "task-2"],
        "dataMappings": {
          "liveData": "task-1.data",
          "orderData": "task-2.data"
        },
        "staticParams": {}
      }
    ]
  },
  "success": true
}
```

**验证点**：
- 返回执行计划但不执行
- 子任务之间的依赖关系正确
- dataMappings定义了参数来源

### 测试3: 强制多Agent模式

**场景**：即使任务简单也使用多Agent模式

**请求**：
```bash
curl -X POST http://localhost:8080/api/v1/chat/send-multi \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test-user-002",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "查询今天的订单数据"
          }
        ]
      }
    ]
  }'
```

**预期行为**：
- 即使任务简单，也会被强制分解为多个子任务
- 可能降级为单个GenericAnalysisAgent

### 测试4: 单Agent模式（对比）

**场景**：简单任务不触发多Agent模式

**请求**：
```bash
curl -X POST http://localhost:8080/api/v1/chat/send \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test-user-003",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "今天天气怎么样"
          }
        ]
      }
    ]
  }'
```

**预期行为**：
- 使用单Agent模式处理
- 响应速度较快
- `multi_agent_execution`表无新记录

---

## 三、数据库验证

### 查询多Agent执行记录

```sql
-- 查看最近的多Agent执行记录
SELECT
    id,
    parent_task_id,
    execution_mode,
    total_latency_ms,
    success_count,
    failure_count,
    status,
    created_at
FROM multi_agent_execution
ORDER BY created_at DESC
LIMIT 10;
```

### 查看子任务详情

```sql
-- 通过父任务ID查询所有子任务
SELECT
    task_id,
    agent_name,
    task_type,
    status,
    latency_ms,
    task_order,
    created_at
FROM task_execution
WHERE parent_task_id = 'multi-task-xxxxx'
ORDER BY task_order;
```

### 使用视图查看完整信息

```sql
-- 使用视图获取多Agent任务及其子任务的完整信息
SELECT * FROM v_multi_agent_task_detail
WHERE parent_task_id = 'multi-task-xxxxx';
```

---

## 四、功能验证清单

### 核心功能验证

- [ ] **任务分解**：复杂任务能被自动分解为多个子任务
- [ ] **Agent选择**：为每个子任务选择合适的Agent
- [ ] **依赖解析**：子任务按正确的依赖顺序执行
- [ ] **参数传递**：前置任务的输出能正确传递给后续任务
- [ ] **结果聚合**：多个Agent的结果能被智能聚合
- [ ] **执行记录**：执行过程被完整记录到数据库

### 降级和容错验证

- [ ] **LLM分解失败**：降级到GenericAnalysisAgent
- [ ] **Agent不存在**：执行计划验证失败，触发降级
- [ ] **循环依赖**：检测并拒绝执行
- [ ] **子任务失败（必须）**：终止后续执行
- [ ] **子任务失败（可选）**：继续执行其他任务
- [ ] **重试机制**：失败任务自动重试（最多3次）

### 性能验证

- [ ] **串行执行耗时**：验证总耗时 ≈ 各子任务耗时之和
- [ ] **并发安全**：CollaborationContext的线程安全性
- [ ] **数据库压力**：多Agent执行不会导致连接池耗尽

---

## 五、日志监控

### 关键日志标识

执行多Agent任务时，关注以下日志：

```
[INFO] 检测到复杂任务,启用多Agent协同模式
[INFO] 开始执行多Agent协同任务: taskId=..., mode=SEQUENTIAL
[INFO] 子任务开始执行: taskId=task-1, agentName=...
[INFO] 子任务执行完成: taskId=task-1, status=..., latency=...ms
[INFO] 聚合结果: successCount=..., failureCount=...
[INFO] 多Agent协同任务执行完成: taskId=..., latency=...ms
```

### 错误日志

```
[ERROR] LLM任务分解失败: ...
[WARN] 执行计划验证失败: ...
[ERROR] 子任务执行失败: taskId=..., error=...
```

---

## 六、性能基准

### 预期性能指标

| 指标 | 预期值 | 说明 |
|------|--------|------|
| 任务分解耗时 | < 5s | LLM调用时间 |
| 子任务执行耗时 | 视Agent而定 | 各Agent独立计时 |
| 结果聚合耗时 | < 3s | LLM调用时间 |
| 数据库写入耗时 | < 100ms | 异步写入不影响响应 |
| 总体响应时间 | 10-30s | 串行执行3-5个Agent |

### 监控SQL

```sql
-- 统计多Agent任务成功率
SELECT
    status,
    COUNT(*) as count,
    AVG(total_latency_ms) as avg_latency,
    AVG(success_count) as avg_success,
    AVG(failure_count) as avg_failure
FROM multi_agent_execution
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY status;

-- 查询最耗时的任务
SELECT
    parent_task_id,
    execution_mode,
    total_latency_ms,
    success_count + failure_count as total_tasks,
    created_at
FROM multi_agent_execution
ORDER BY total_latency_ms DESC
LIMIT 10;

-- 统计每个Agent的使用频率
SELECT
    agent_name,
    COUNT(*) as execution_count,
    AVG(latency_ms) as avg_latency,
    SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) as success_count,
    SUM(CASE WHEN status = 'failed' THEN 1 ELSE 0 END) as failure_count
FROM task_execution
WHERE is_sub_task = TRUE
    AND created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY agent_name
ORDER BY execution_count DESC;
```

---

## 七、调试技巧

### 1. 使用预览端点调试任务分解

在实际执行前，先调用`/preview-plan`查看任务会如何被分解：

```bash
curl -X POST http://localhost:8080/api/v1/chat/preview-plan \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": [{"type": "text", "text": "你的测试输入"}]}]}'
```

### 2. 检查执行历史

查看`CollaborationContext`中的执行历史（通过日志）：

```
[DEBUG] 执行历史: [
  {taskId: task-1, agentName: Agent1, success: true, latency: 100ms},
  {taskId: task-2, agentName: Agent2, success: true, latency: 200ms}
]
```

### 3. 验证数据传递

检查日志中的参数提取信息：

```
[DEBUG] 提取参数: inputData = task-1.data.outputValue => "extracted-value-123"
```

### 4. 分析失败原因

查询失败任务的详细错误信息：

```sql
SELECT
    parent_task_id,
    agent_name,
    error_message,
    retry_count,
    created_at
FROM task_execution
WHERE status = 'failed'
    AND is_sub_task = TRUE
ORDER BY created_at DESC;
```

---

## 八、常见问题排查

### Q1: 任务未触发多Agent模式

**可能原因**：
- LLM判断任务不需要协同
- TaskOrchestrator.needsCollaboration() 返回false

**排查方法**：
1. 查看日志中的协同判断结果
2. 使用更明确的复杂任务描述（如"对比"、"分析...和..."）
3. 使用`/send-multi`强制多Agent模式

### Q2: 执行计划验证失败

**可能原因**：
- Agent不存在
- 存在循环依赖
- 参数映射错误

**排查方法**：
1. 调用`/preview-plan`查看生成的计划
2. 检查日志中的验证错误信息
3. 确认所有Agent已正确注册

### Q3: 子任务执行失败

**可能原因**：
- Agent内部错误
- 参数提取失败
- 依赖任务未完成

**排查方法**：
1. 查看`task_execution`表的错误信息
2. 检查`CollaborationContext`中的数据是否正确
3. 验证参数映射路径是否正确

### Q4: 结果聚合失败

**可能原因**：
- LLM调用失败
- 子任务结果格式不符合预期

**排查方法**：
1. 检查LLM调用日志
2. 验证是否使用了模板降级
3. 查看各子任务的返回数据结构

---

## 九、扩展测试场景

### 场景1: 数据分析管道

**输入**：
```
获取昨天的直播数据，计算核心指标（GMV、观看人数、转化率），并生成分析报告
```

**预期执行流程**：
```
task-1: LiveDataFetchAgent -> 获取原始数据
task-2: LiveDataPrepAgent -> 计算指标 (依赖task-1)
task-3: LiveAnalysisAgent -> 生成报告 (依赖task-2)
```

### 场景2: 多数据源对比

**输入**：
```
对比昨天和前天的直播表现和订单数据，找出差异原因
```

**预期执行流程**：
```
task-1: LiveDataFetchAgent(昨天) -> 获取昨天直播数据
task-2: LiveDataFetchAgent(前天) -> 获取前天直播数据
task-3: OrderDataAgent(昨天) -> 获取昨天订单数据
task-4: OrderDataAgent(前天) -> 获取前天订单数据
task-5: GenericAnalysisAgent -> 对比分析 (依赖所有前置任务)
```

### 场景3: 可选任务容错

**输入**：
```
分析直播数据，如果有商品信息也一起分析
```

**预期执行流程**：
```
task-1: LiveDataFetchAgent -> 获取直播数据 (必须)
task-2: ProductDataAgent -> 获取商品数据 (可选，失败不影响整体)
task-3: LiveAnalysisAgent -> 生成报告 (依赖task-1，尽量包含task-2)
```

---

## 十、下一步优化建议

### Phase 2 功能（待实现）

1. **并行执行策略**：独立任务并发执行，提升性能
2. **层级执行策略**：主Agent动态调用子Agent
3. **异步执行模式**：长任务返回taskId，客户端轮询
4. **进度推送**：WebSocket实时推送子任务进度
5. **执行计划缓存**：高频任务计划缓存优化
6. **动态重试策略**：根据错误类型选择重试策略
7. **子任务结果缓存**：避免重复执行相同任务

### 监控指标建议

- 多Agent任务成功率
- 平均执行时长分布
- 各Agent使用频率和失败率
- LLM调用延迟
- 数据库操作耗时

---

## 附录：API参考

### 1. POST /api/v1/chat/send

自动检测是否需要多Agent协同。

**请求体**：
```json
{
  "userId": "user123",
  "sessionId": "session-abc",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "用户输入"}
      ]
    }
  ],
  "context": {}
}
```

**响应**：
```json
{
  "message": "聚合后的结果摘要",
  "success": true,
  "data": {
    "subTaskResults": [...],
    "executionPlan": {...}
  },
  "routingInfo": {
    "selectedAgent": "...",
    "confidence": 0.9
  }
}
```

### 2. POST /api/v1/chat/preview-plan

预览执行计划，不实际执行。

**请求体**：
```json
{
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "用户输入"}
      ]
    }
  ]
}
```

**响应**：
```json
{
  "plan": {
    "planId": "plan-xxx",
    "executionMode": "SEQUENTIAL",
    "reason": "...",
    "subTasks": [...]
  },
  "success": true
}
```

### 3. POST /api/v1/chat/send-multi

强制使用多Agent模式。

**请求体**：同 `/send`

**响应**：同 `/send`

---

**版本**: 1.0
**最后更新**: 2025-12-18
**作者**: 神工开发团队
